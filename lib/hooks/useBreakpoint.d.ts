import { Breakpoint, BreakpointValues } from '../types';
/**
 * Custom hook to determine the current breakpoint based on screen width
 */
export declare const useBreakpoint: (customBreakpoints?: Partial<BreakpointValues>) => {
    breakpoint: Breakpoint;
    screenWidth: number;
    screenHeight: number;
};
//# sourceMappingURL=useBreakpoint.d.ts.map